# AltFinder Project Instructions

## Git Push

This repo uses the **opentoolshub** GitHub account (not tmad4000).

The origin remote is configured with SSH alias `github-opentoolshub`:
```
origin  git@github-opentoolshub:opentoolshub/AltFinder.git
```

Just use `git push` normally - the SSH config handles the account switching.
